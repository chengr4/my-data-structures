# Hash Map

```golang
myMap := make(map[int]int)

// iterate elements in the map
for k, v := range countMap {
  // do something
}

myMap = append(myMap, 123)  // append
myMap = myMap[:len(myMap)-1] // pop
```